| Категория  | Событие                    |
| ---------- | -------------------------- |
| Space      | Создание DAO               |
| Space      | Изменение стратегий        |
| Space      | Изменение admins / members |
| Proposal   | Создание proposal          |
| Proposal   | Активация голосования      |
| Proposal   | Закрытие                   |
| Proposal   | Отмена                     |
| Voting     | Голос подан                |
| Voting     | Переголосование            |
| Voting     | Подсчёт VP                 |
| Execution  | SafeSnap исполнение        |
| Delegation | Делегирование              |
| Delegation | Отзыв делегирования        |
# 1️⃣ События Space (DAO-level events)

## 🏠 Создание / изменение Space

Snapshot не имеет отдельного REST-event API,  
НО **все изменения Space фиксируются как messages**.

### Что можно отследить:

- создание Space    
- изменение стратегий    
- смена admins / members    
- изменение filters    
- подключение / отключение plugins    
- изменение voting parameters    

### Как получить:

`query {   messages(     where: {       space: "saffronfinance.eth"     }     orderBy: "created"     orderDirection: asc   ) {     id     type     author     created     payload   } }`

### Типичные `type`:

- `settings` — изменение настроек Space    
- `space` — создание / обновление space    

📌 **Это “audit log” DAO.**

---

# 2️⃣ События Proposals (core governance)

## 🗳 Создание proposal

### Событие:
- новый proposal опубликован    

### Где:
- `proposals`    

`query {   proposals(where: { space: "saffronfinance.eth" }) {     id     title     author     created     start     end     state   } }`

### Это governance-событие:
- инициатива    
- начало обсуждения    
- запуск голосования    

---

## ⏱ Изменение состояния proposal

Proposal автоматически проходит состояния:

`pending → active → closed`

Поле:

`"state": "active"`

📌 Это **временное governance-событие**, не ручное.

---

## ❌ Отмена proposal

- Proposal может быть отменён автором / админом    
- Состояние: `canceled`    

Это тоже governance-событие.

---

# 3️⃣ События голосования (Voting events)

## 🗳 Vote cast (самое важное)

### Событие:

- пользователь проголосовал    

### Где:

- `votes`    

`query {   votes(     where: { proposal: "0x..." }   ) {     voter     choice     vp     created   } }`

### Что это означает:

- участие в управлении    
- фиксируется навсегда (off-chain, но подписано)    

---

## 🔄 Изменение голоса

Snapshot **разрешает переголосование**:

- последний голос заменяет предыдущий    
- старый голос остаётся в истории    

📌 Это тоже governance-событие.

---

## ⚖ Подсчёт voting power

Каждый vote содержит:

`"vp": 123.45`

Это:

- результат применения стратегий    
- на момент snapshot-блока    

---

# 4️⃣ Исполнение решений (Execution events)

⚠️ Важно: **Snapshot сам не исполняет on-chain**,  
но может быть связан с execution-механизмами.

---

## 🔐 SafeSnap execution (если подключён)

Если подключён `plugins.safeSnap`:

### Governance-события:

- proposal approved    
- transaction queued    
- transaction executed    

Часть данных:

- в Snapshot (`plugins`)    
- часть — **в Gnosis Safe (on-chain)**    

📌 Snapshot API даёт:

- результат голосования    
- metadata    
- связь с Safe    

---

# 5️⃣ Делегирование (Delegation events)

Если DAO использует delegation:

### Где:

- `delegations`    
- `delegate-registry`    

### События:

- делегировал голос    
- отозвал делегирование    
- сменил делегата    

Это **meta-governance события**.

---

# 6️⃣ Follow / Social events (вторичные)

### Follow Space

`follows {   follower   space }`

Это не governance напрямую, но:

- показывает вовлечённость    
- используется в аналитике